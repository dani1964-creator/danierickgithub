import { ReactNode } from 'react';
interface PageTransitionProps {
    children: ReactNode;
    duration?: number;
}
export declare const PageTransition: ({ children, duration }: PageTransitionProps) => import("react/jsx-runtime").JSX.Element;
export {};
