# Local env example for frontend (do NOT commit real secrets)
# Copy this file to `.env.local` and fill values before running dev server

# Public (safe to expose in client builds)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only (do NOT prefix with NEXT_PUBLIC_). Keep this out of the browser.
# Use in server scripts or CI jobs that require admin privileges.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional
# NEXT_PUBLIC_BASE_DOMAIN=example.com